{
    "name": "githubtask",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "npm run build && node ./dist/server/index",
        "start:dev": "npm run build && nodemon ./dist/server/index --exec babel-node --presets babel-preset-env",
        "build": "babel src -d dist",
        "test": "jest --verbose"
    },
    "dependencies": {
        "cookie-parser": "~1.4.4",
        "debug": "~2.6.9",
        "express": "~4.16.1",
        "http-errors": "~1.6.3",
        "jade": "~1.11.0",
        "morgan": "~1.9.1"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-core": "^6.26.3",
        "babel-loader": "^8.0.6",
        "babel-preset-env": "^1.7.0",
        "nodemon": "^2.0.2"
    }
}
